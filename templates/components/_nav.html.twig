<nav class="navbar navbar-expand-lg justify-content-center bg-dark">
    <div class="navbar-nav justify-content-around">
        <div class="nav-item d-flex align-items-center">
            <img src="{{ asset('build/images/warframelogo.png') }}" alt="Logo warframe" width="90">
            <a class="nav-link" aria-current="page" href="{{ path('home') }}">Home</a>
        </div>
        <div class="nav-item d-flex align-items-center">
            <a class="nav-link" href="{{ path('builder') }}">Builder</a>
        </div>
        {% if is_granted('ROLE_ADMIN') %}
            <div class="nav-item d-flex align-items-center">
                <a class="nav-link" href="{{ path('admin') }}">Admin dashboard</a>
            </div>
        {% endif %}
        {% if app.user %}
            <div class="nav-item d-flex align-items-center">
                <a class="nav-link" href="{{ path('app_logout') }}">logout</a>
            </div>
        {% else %}
            <div class="nav-item d-flex align-items-center">
                <a class="nav-link" href="{{ path('app_register') }}">Registration</a>
            </div>
            <div class="nav-item d-flex align-items-center">
                <a class="nav-link" href="{{ path('login') }}">Login</a>
            </div>
        {% endif %}
    </div>
</nav>
<div class="text-center mt-4 mb-3">
    <img class="text-center" src="http://warframe-builder.com/web/images/logo_warframe_builder.png" alt="">
</div>
