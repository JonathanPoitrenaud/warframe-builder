<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">

            <ul class="navbar-nav me-auto mb-2 mb-lg-0 justify-content-between">
                <div class="nav-item d-flex align-items-center">
                    <img src="{{ asset('build/images/warframelogo.png') }}" alt="Logo warframe" width="90">
                    <a class="nav-link active" aria-current="page" href="#">Home</a>
                </div>
                <div class="nav-item d-flex align-items-center">
                    <a class="nav-link" href="#">Builder</a>
                </div>
                <div class="nav-item d-flex align-items-center">
                    <a class="nav-link" href="{{ path('app_register') }}">Registration</a>
                </div>
                {% if is_granted('ROLE_ADMIN') %}
                    <div class="nav-item d-flex align-items-center">
                        <a class="nav-link" href="#">Admin dashboard</a>
                    </div>
                {% endif %}
                {% if app.user %}
                    <div class="nav-item d-flex align-items-center">
                        <a class="nav-link" href="{{ path('app_logout') }}">logout</a>
                    </div>
                {% else %}
                    <div class="nav-item d-flex align-items-center">
                        <a class="nav-link" href="{{ path('login') }}">Login</a>
                    </div>
                {% endif %}
            </ul>

    </div>
</nav>